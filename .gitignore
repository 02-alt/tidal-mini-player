build/
dist/
.DS_Store

# Conductor workspace scratch (also holds the local signing private key)
.context/

# Never commit signing material
*.pem
*.p12

# Vendored Sparkle (auto-downloaded by build.sh)
ThirdParty/Sparkle/

# Sparkle release staging (archives; appcast.xml at repo root is tracked)
releases/
